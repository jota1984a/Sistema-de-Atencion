@model IEnumerable<ProyectoNAtencion.Models.ESTADO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPCION_ESTADO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION_ESTADO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_ESTADO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_ESTADO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_ESTADO })
        </td>
    </tr>
}

</table>
